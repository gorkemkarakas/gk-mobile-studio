const translations = {
  en: {
    "meta.title": "GK Mobile Studio | Digital products that move business forward",
    "meta.description": "GK Mobile Studio creates mobile apps, dashboards and web solutions for growing businesses.",
    "accessibility.skip": "Skip to main content",
    "accessibility.openNav": "Open navigation",
    "accessibility.primaryNav": "Primary navigation",
    "accessibility.footerNav": "Footer navigation",
    "accessibility.servicesNav": "Services",
    "accessibility.legalNav": "Legal",
    "language.label": "Language",
    "nav.home": "Home",
    "nav.about": "About",
    "nav.services": "Services",
    "nav.projects": "Projects",
    "nav.contact": "Contact",
    "cta.quote": "Get a Quote",
    "cta.viewWork": "View Work",
    "hero.eyebrow": "Welcome to GK Mobile Studio",
    "hero.title": "Crafting Digital <span>Success</span>",
    "hero.text": "We build mobile apps, dashboards, and web solutions that help businesses grow.",
    "hero.imageAlt": "Mobile analytics dashboard displayed on a smartphone and laptop",
    "services.title": "Services",
    "services.intro": "End-to-end digital solutions tailored to your business needs.",
    "services.mobile.title": "Mobile Apps",
    "services.mobile.text": "High-performance, user-friendly mobile apps for iOS & Android.",
    "services.web.title": "Web Development",
    "services.web.text": "Responsive, fast, and SEO-friendly web applications that scale with you.",
    "services.dashboard.title": "Dashboards",
    "services.dashboard.text": "Data-driven dashboards with real-time insights and smart reporting.",
    "services.mvp.title": "MVP Development",
    "services.mvp.text": "Validate your idea with a minimum viable product built to adapt and grow.",
    "projects.title": "Projects",
    "projects.viewAll": "View All Projects",
    "projects.mobileCategory": "Mobile App",
    "projects.dashboardCategory": "Dashboard",
    "projects.webCategory": "Web App",
    "projects.tripledgerAlt": "TripLedger travel app preview",
    "projects.tripledgerText": "A travel expense tracker that helps users plan trips, track expenses and manage receipts on the go.",
    "projects.caseStudy": "View Case Study",
    "projects.deviceAlt": "DeviceCloud analytics dashboard mockup",
    "projects.deviceText": "A device management dashboard for monitoring systems, metrics and remote configuration at scale.",
    "projects.posAlt": "Restaurant POS interface mockup",
    "projects.todaySales": "Today’s Sales",
    "projects.posText": "A modern POS system for restaurants with seamless ordering, inventory, and sales reporting.",
    "legal.privacyShort": "Privacy",
    "legal.termsShort": "Terms",
    "about.eyebrow": "About us",
    "about.title": "Digital products with a human touch.",
    "about.text": "GK Mobile Studio is an independent team focused on making useful digital experiences feel clear, capable, and easy to use. We work closely with each client from first idea to launch and beyond.",
    "contact.eyebrow": "Let’s work together",
    "contact.title": "Have a project in mind?",
    "contact.button": "Start a Conversation",
    "footer.description": "Building mobile apps, dashboards and web products that move businesses forward.",
    "footer.quickLinks": "Quick Links",
    "footer.services": "Services",
    "footer.mobileService": "Mobile App Development",
    "footer.webService": "Web Development",
    "footer.dashboardService": "Dashboard Development",
    "footer.mvpService": "MVP Development",
    "footer.ctaTitle": "Let’s Work Together",
    "footer.ctaText": "Let’s turn your idea into a clear, fast and scalable digital product.",
    "footer.ctaButton": "Get in Touch",
    "footer.rights": "All rights reserved.",
    "footer.privacy": "Privacy Policy",
    "footer.terms": "Terms of Service",
    "social.github": "GitHub",
    "social.linkedin": "LinkedIn",
    "social.email": "Email"
  },
  tr: {
    "meta.title": "GK Mobile Studio | İşletmeleri ileri taşıyan dijital ürünler",
    "meta.description": "GK Mobile Studio, büyüyen işletmeler için mobil uygulamalar, yönetim panelleri ve web çözümleri geliştirir.",
    "accessibility.skip": "Ana içeriğe geç",
    "accessibility.openNav": "Menüyü aç",
    "accessibility.primaryNav": "Ana menü",
    "accessibility.footerNav": "Footer bağlantıları",
    "accessibility.servicesNav": "Hizmetler",
    "accessibility.legalNav": "Yasal bağlantılar",
    "language.label": "Dil",
    "nav.home": "Ana Sayfa",
    "nav.about": "Hakkımızda",
    "nav.services": "Hizmetler",
    "nav.projects": "Projeler",
    "nav.contact": "İletişim",
    "cta.quote": "Teklif Al",
    "cta.viewWork": "Projeleri Gör",
    "hero.eyebrow": "GK Mobile Studio'ya Hoş Geldiniz",
    "hero.title": "Dijital Başarıyı <span>İnşa Ediyoruz.</span>",
    "hero.text": "İşletmelerin büyümesine yardımcı olan mobil uygulamalar, yönetim panelleri ve web çözümleri geliştiriyoruz.",
    "hero.imageAlt": "Akıllı telefon ve dizüstü bilgisayarda gösterilen mobil analiz paneli",
    "services.title": "Hizmetler",
    "services.intro": "İşletmenizin ihtiyaçlarına özel uçtan uca dijital çözümler.",
    "services.mobile.title": "Mobil Uygulamalar",
    "services.mobile.text": "iOS ve Android için yüksek performanslı, kullanıcı dostu mobil uygulamalar.",
    "services.web.title": "Web Geliştirme",
    "services.web.text": "İşletmenizle birlikte büyüyen, hızlı, responsive ve SEO uyumlu web uygulamaları.",
    "services.dashboard.title": "Yönetim Panelleri",
    "services.dashboard.text": "Gerçek zamanlı içgörüler ve akıllı raporlama sunan veri odaklı paneller.",
    "services.mvp.title": "MVP Geliştirme",
    "services.mvp.text": "Fikrinizi uyarlanabilir ve büyümeye hazır bir minimum uygulanabilir ürünle doğrulayın.",
    "projects.title": "Projeler",
    "projects.viewAll": "Tüm Projeleri Gör",
    "projects.mobileCategory": "Mobil Uygulama",
    "projects.dashboardCategory": "Yönetim Paneli",
    "projects.webCategory": "Web Uygulaması",
    "projects.tripledgerAlt": "TripLedger seyahat uygulaması önizlemesi",
    "projects.tripledgerText": "Kullanıcıların seyahat planlamasına, harcamalarını takip etmesine ve fişlerini yönetmesine yardımcı olan bir seyahat gider takip uygulaması.",
    "projects.caseStudy": "Proje Detayını Gör",
    "projects.deviceAlt": "DeviceCloud analiz paneli maketi",
    "projects.deviceText": "Sistemleri, metrikleri ve uzaktan yapılandırmayı ölçekli olarak izlemek için geliştirilmiş bir cihaz yönetim paneli.",
    "projects.posAlt": "Restoran POS arayüzü maketi",
    "projects.todaySales": "Bugünkü Satışlar",
    "projects.posText": "Sipariş, stok ve satış raporlamasını bir araya getiren modern bir restoran POS sistemi.",
    "legal.privacyShort": "Gizlilik",
    "legal.termsShort": "Koşullar",
    "about.eyebrow": "Hakkımızda",
    "about.title": "İnsan odaklı dijital ürünler.",
    "about.text": "GK Mobile Studio; kullanışlı dijital deneyimleri anlaşılır, güçlü ve kolay kullanılabilir hale getirmeye odaklanan bağımsız bir ekiptir. İlk fikirden yayına ve sonrasına kadar her müşterimizle yakın çalışırız.",
    "contact.eyebrow": "Birlikte çalışalım",
    "contact.title": "Aklınızda bir proje mi var?",
    "contact.button": "Görüşme Başlat",
    "footer.description": "İşletmeleri ileri taşıyan mobil uygulamalar, yönetim panelleri ve web ürünleri geliştiriyoruz.",
    "footer.quickLinks": "Hızlı Bağlantılar",
    "footer.services": "Hizmetler",
    "footer.mobileService": "Mobil Uygulama Geliştirme",
    "footer.webService": "Web Geliştirme",
    "footer.dashboardService": "Yönetim Paneli Geliştirme",
    "footer.mvpService": "MVP Geliştirme",
    "footer.ctaTitle": "Birlikte Çalışalım",
    "footer.ctaText": "Fikrinizi net, hızlı ve ölçeklenebilir bir dijital ürüne dönüştürelim.",
    "footer.ctaButton": "İletişime Geç",
    "footer.rights": "Tüm hakları saklıdır.",
    "footer.privacy": "Gizlilik Politikası",
    "footer.terms": "Kullanım Şartları",
    "social.github": "GitHub",
    "social.linkedin": "LinkedIn",
    "social.email": "E-posta"
  }
};

if (!window.I18N) {
  window.I18N = translations;
}

const header = document.querySelector("[data-header]");
const revealItems = document.querySelectorAll(".reveal");
const prefersReducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
const isFinePointer = window.matchMedia("(pointer: fine)").matches;

document.querySelectorAll("[data-current-year]").forEach((element) => {
  element.textContent = new Date().getFullYear();
});

const updateHeader = () => {
  header?.classList.toggle("is-scrolled", window.scrollY > 18);
};

updateHeader();
window.addEventListener("scroll", updateHeader, { passive: true });

const menuToggle = document.querySelector("[data-menu-toggle]");
const headerMenu = document.querySelector("[data-header-menu]");

const closeMenu = () => {
  if (!menuToggle || !header) return;
  menuToggle.setAttribute("aria-expanded", "false");
  header.classList.remove("menu-open");
};

menuToggle?.addEventListener("click", () => {
  const willOpen = menuToggle.getAttribute("aria-expanded") !== "true";
  menuToggle.setAttribute("aria-expanded", String(willOpen));
  header?.classList.toggle("menu-open", willOpen);
});

headerMenu?.querySelectorAll("a").forEach((link) => {
  link.addEventListener("click", closeMenu);
});

document.addEventListener("keydown", (event) => {
  if (event.key === "Escape") {
    closeMenu();
    menuToggle?.focus();
  }
});

document.addEventListener("click", (event) => {
  if (header?.classList.contains("menu-open") && !header.contains(event.target)) {
    closeMenu();
  }
});

window.addEventListener("resize", () => {
  if (window.innerWidth > 800) closeMenu();
});

if ("IntersectionObserver" in window) {
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("is-visible");
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.14 }
  );

  revealItems.forEach((item) => observer.observe(item));
} else {
  revealItems.forEach((item) => item.classList.add("is-visible"));
}

// Back to top button
const toTopButton = document.querySelector("[data-to-top]");

if (toTopButton) {
  const updateToTop = () => {
    toTopButton.classList.toggle("is-visible", window.scrollY > 480);
  };

  updateToTop();
  window.addEventListener("scroll", updateToTop, { passive: true });
  toTopButton.addEventListener("click", () => {
    window.scrollTo({ top: 0, behavior: prefersReducedMotion ? "auto" : "smooth" });
  });
}

// Ambient cursor glow
const cursorGlow = document.querySelector("[data-cursor-glow]");

if (cursorGlow && isFinePointer && !prefersReducedMotion) {
  let hasShown = false;

  window.addEventListener(
    "pointermove",
    (event) => {
      cursorGlow.style.transform = `translate3d(${event.clientX}px, ${event.clientY}px, 0)`;
      if (!hasShown) {
        cursorGlow.classList.add("is-active");
        hasShown = true;
      }
    },
    { passive: true }
  );

  window.addEventListener("pointerleave", () => cursorGlow.classList.remove("is-active"));
}

// Phone mockup tilt
const tiltTarget = document.querySelector("[data-tilt]");
const tiltZone = document.querySelector(".hero-visual");

if (tiltTarget && tiltZone && isFinePointer && !prefersReducedMotion) {
  const maxTilt = 9;

  tiltZone.addEventListener("pointermove", (event) => {
    const bounds = tiltZone.getBoundingClientRect();
    const relativeX = (event.clientX - bounds.left) / bounds.width - 0.5;
    const relativeY = (event.clientY - bounds.top) / bounds.height - 0.5;
    const rotateY = relativeX * maxTilt * 2;
    const rotateX = relativeY * -maxTilt * 2;
    tiltTarget.style.transform = `perspective(1400px) rotateX(${rotateX}deg) rotateY(${rotateY}deg)`;
  });

  tiltZone.addEventListener("pointerleave", () => {
    tiltTarget.style.transform = "perspective(1400px) rotateX(0deg) rotateY(0deg)";
  });
}

// Magnetic buttons
const magneticButtons = document.querySelectorAll("[data-magnetic]");

if (isFinePointer && !prefersReducedMotion) {
  magneticButtons.forEach((button) => {
    const strength = 0.28;

    button.addEventListener("pointermove", (event) => {
      const bounds = button.getBoundingClientRect();
      const offsetX = event.clientX - (bounds.left + bounds.width / 2);
      const offsetY = event.clientY - (bounds.top + bounds.height / 2);
      button.style.transform = `translate(${offsetX * strength}px, ${offsetY * strength}px)`;
    });

    button.addEventListener("pointerleave", () => {
      button.style.transform = "";
    });
  });
}

// Stat count-up
const statNumbers = document.querySelectorAll("[data-count-to]");

const currentSuffix = (element) => {
  const lang = document.documentElement.lang === "tr" ? "tr" : "en";
  return (lang === "tr" && element.dataset.suffixTr) || element.dataset.suffix || "";
};

const animateCount = (element) => {
  const target = Number(element.dataset.countTo);
  const suffix = currentSuffix(element);

  if (prefersReducedMotion) {
    element.textContent = `${target}${suffix}`;
    element.dataset.animated = "true";
    return;
  }

  const duration = 1400;
  const start = performance.now();

  const step = (now) => {
    const progress = Math.min((now - start) / duration, 1);
    const eased = 1 - Math.pow(1 - progress, 3);
    const value = Math.round(target * eased);
    element.textContent = `${value}${currentSuffix(element)}`;

    if (progress < 1) {
      requestAnimationFrame(step);
    } else {
      element.dataset.animated = "true";
    }
  };

  requestAnimationFrame(step);
};

if ("IntersectionObserver" in window && statNumbers.length) {
  const statObserver = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          animateCount(entry.target);
          statObserver.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.5 }
  );

  statNumbers.forEach((item) => statObserver.observe(item));
} else {
  statNumbers.forEach((item) => animateCount(item));
}

// Language toggle (EN / TR)
const LANG_KEY = "gk-lang";

const getInitialLang = () => {
  const saved = localStorage.getItem(LANG_KEY);
  return saved === "tr" ? "tr" : "en";
};

const ATTR_MAP = {
  "data-i18n-aria-label": "aria-label",
  "data-i18n-alt": "alt",
  "data-i18n-title": "title",
  "data-i18n-content": "content",
};

const applyLanguage = (lang) => {
  const dict = (window.I18N && window.I18N[lang]) || {};
  document.documentElement.lang = lang;

  document.querySelectorAll("[data-i18n]").forEach((el) => {
    const key = el.getAttribute("data-i18n");
    if (dict[key] !== undefined) el.textContent = dict[key];
  });

  document.querySelectorAll("[data-i18n-html]").forEach((el) => {
    const key = el.getAttribute("data-i18n-html");
    if (dict[key] !== undefined) el.innerHTML = dict[key];
  });

  Object.entries(ATTR_MAP).forEach(([dataAttr, targetAttr]) => {
    document.querySelectorAll(`[${dataAttr}]`).forEach((el) => {
      const key = el.getAttribute(dataAttr);
      if (dict[key] !== undefined) el.setAttribute(targetAttr, dict[key]);
    });
  });

  statNumbers.forEach((el) => {
    if (el.dataset.animated === "true") {
      el.textContent = `${el.dataset.countTo}${currentSuffix(el)}`;
    }
  });

  document.querySelectorAll("[data-lang-toggle] button").forEach((btn) => {
    const isActive = btn.dataset.lang === lang;
    btn.classList.toggle("is-active", isActive);
    btn.setAttribute("aria-pressed", String(isActive));
  });
};

const langToggle = document.querySelector("[data-lang-toggle]");

if (langToggle) {
  applyLanguage(getInitialLang());

  langToggle.querySelectorAll("button").forEach((btn) => {
    btn.addEventListener("click", () => {
      const chosen = btn.dataset.lang;
      localStorage.setItem(LANG_KEY, chosen);
      applyLanguage(chosen);
    });
  });
}
